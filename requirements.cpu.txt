# =============================================================================
# CPU-only runtime requirements for deploying PDFlow (e.g. Hugging Face Spaces).
#
# Derived from requirements.txt with the GPU/dev weight stripped out:
#   - paddlepaddle-gpu + the whole nvidia-* CUDA 12 stack  -> removed
#   - torch / torchvision / triton (unused by the OCR path) -> removed
#   - Jupyter / IPython / debug tooling                     -> removed
#
# IMPORTANT: paddlepaddle (CPU build) is installed separately in the Dockerfile
# from Paddle's CPU wheel index, BEFORE this file, so paddleocr/paddlex find it
# already satisfied. Do not add paddlepaddle here (the 3.2.1 CPU wheel is not on
# PyPI). For a GPU deploy, use requirements.txt instead of this file.
# =============================================================================

# --- Core ---
anyio==4.12.1
certifi==2026.1.4
charset-normalizer==3.4.4
click==8.3.1
decorator==5.2.1
filelock==3.20.3
future==1.0.0
idna==3.11
packaging==26.0
requests==2.32.5
setuptools==82.0.1
six==1.17.0
typing-extensions==4.15.0
typing-inspection==0.4.2
urllib3==2.6.3

# --- Web layer (from backend/requirements.txt) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- PDF processing ---
PyMuPDF==1.26.7
pdf2docx==0.5.9
pikepdf==10.3.0
pypdfium2==5.6.0
python-docx==1.2.0
puremagic==1.30

# --- OCR & document intelligence ---
paddleocr==3.4.0
paddlex==3.4.2
pytesseract==0.3.13
python-bidi==0.6.7
pyclipper==1.4.0

# --- Computer vision & imaging (single headless OpenCV, no GUI libs) ---
opencv-contrib-python-headless==4.10.0.84
pillow==12.1.0
shapely==2.1.2

# --- Model hub / serialization ---
safetensors==0.7.0
huggingface-hub==1.7.1
hf-xet==1.4.2
modelscope==1.35.0

# --- Numerical ---
numpy==2.4.2
pandas==3.0.0
scipy==1.17.0
networkx==3.6.1

# --- Config / serialization ---
PyYAML==6.0.2
ruamel.yaml==0.19.1
protobuf==6.33.4
ujson==5.12.0
configobj==5.0.9
configparser==7.2.0

# --- Runtime utilities ---
tqdm==4.67.3
rich==14.3.3
colorlog==6.10.1
termcolor==3.3.0
fire==0.7.1
typer==0.24.1
shellingham==1.5.4
prettytable==3.17.0
fonttools==4.61.1

# --- System / low-level ---
psutil==7.2.2
platformdirs==4.9.4
wcwidth==0.6.0
wrapt==2.1.1
Deprecated==1.3.1
